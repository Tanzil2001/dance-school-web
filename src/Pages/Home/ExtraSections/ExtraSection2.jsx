import React from 'react';
import bg from '../../../assets/combine-img-1.jpg';

const ExtraSection2 = () => {
    return (
        <div style={{backgroundImage: `url("${bg}")`, backgroundSize: 'cover', height: '100vh', backgroundPosition: 'center'}}>
            
            
        </div>
    );
};

export default ExtraSection2;